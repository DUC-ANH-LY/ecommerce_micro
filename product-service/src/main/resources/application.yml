spring:
  application:
    name: product-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/product_db
    redis:
      host: localhost
      port: 6379
  elasticsearch:
    uris: http://localhost:9200
  cache:
    type: redis

server:
  port: 8080
